/**
 * @deepseek-ai/dsh-spawn-model-choice — ask the human which model + reasoning
 * effort a spawned subagent should use. The package's substance is
 * `cordis.patch.yml` (resolved via `dsh.bundle.patch`) and the
 * `src/spawn-model-choice.mjs` Cordis plugin; this module carries no runtime
 * API beyond re-exporting the patch identity.
 * @module @deepseek-ai/dsh-spawn-model-choice
 */

export {}
